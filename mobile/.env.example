# Copy to .env and adjust per environment. Never commit .env.
# EXPO_PUBLIC_ prefixed vars are inlined at build time and readable in the
# shipped app bundle — fine for a public API base URL, never for secrets.
EXPO_PUBLIC_API_BASE_URL=http://localhost:8000/api/v1
